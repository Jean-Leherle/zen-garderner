const express = require('express');
const app = express();

// app.use(express.json());

// Dimitri m'a donné ça pour jwt, à voir 
// const bodyParser = require('body-parser');
// app.use(bodyParser.json());
module.exports = app;